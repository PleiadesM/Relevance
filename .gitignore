# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Local env & credentials — never commit
.env
*.env
!examples/advanced-sources.env.example
feeds/follow.opml
config/*.local.json
ics-sources.json
examples/ics-sources.json

# OS / editor
.DS_Store
Thumbs.db

# Local scratch build output (the real data/ IS committed by Actions)
/data-local/

# Source Studio — local, offline source-editing workspace; never committed.
# The generated page and the plan it emits stay on the deployer's machine.
/newsdash-studio/
source-studio*.html
*.plan.json

# Session handoff notes — personal working notes, not project history
HANDOFF.md
